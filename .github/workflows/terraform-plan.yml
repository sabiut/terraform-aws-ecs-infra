name: 'Terraform Plan'

on:
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'terraform/**'
      - '.github/workflows/terraform-plan.yml'

env:
  TERRAFORM_VERSION: '1.5.7'
  WORKING_DIR: './terraform'
  AWS_REGION: 'ap-southeast-2'
  # Pull request plans run against the dev workspace.
  TF_ENVIRONMENT: dev
  TF_VAR_environment: dev

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-plan:
    name: 'Terraform Plan'
    runs-on: ubuntu-latest
    # The secrets context is not available in step "if" conditions, so the
    # checks are evaluated here and the steps test these variables instead.
    env:
      HAS_AWS_CREDENTIALS: ${{ secrets.AWS_ACCESS_KEY_ID != '' }}
      HAS_STATE_BUCKET: ${{ secrets.TERRAFORM_STATE_BUCKET != '' }}

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: ${{ env.TERRAFORM_VERSION }}

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      if: env.HAS_AWS_CREDENTIALS == 'true'
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Terraform Init
      id: init
      run: |
        if [ -n "${{ secrets.TERRAFORM_STATE_BUCKET }}" ]; then
          terraform init \
            -backend-config="bucket=${{ secrets.TERRAFORM_STATE_BUCKET }}" \
            -backend-config="key=terraform.tfstate" \
            -backend-config="region=${{ env.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TERRAFORM_LOCK_TABLE || 'terraform-state-locks' }}" \
            -backend-config="encrypt=true"
        else
          terraform init -backend=false
        fi
      working-directory: ${{ env.WORKING_DIR }}

    - name: Select Workspace
      if: env.HAS_STATE_BUCKET == 'true'
      run: |
        terraform workspace select ${{ env.TF_ENVIRONMENT }} || terraform workspace new ${{ env.TF_ENVIRONMENT }}
      working-directory: ${{ env.WORKING_DIR }}

    - name: Terraform Plan
      id: plan
      run: |
        if [ -n "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then
          terraform plan -no-color -out=tfplan
        else
          echo "Skipping plan - AWS credentials not configured"
          echo "Please add AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY to repository secrets"
          exit 0
        fi
      working-directory: ${{ env.WORKING_DIR }}
      continue-on-error: true

    - name: Terraform Show Plan
      id: show
      if: env.HAS_AWS_CREDENTIALS == 'true'
      run: terraform show -no-color tfplan
      working-directory: ${{ env.WORKING_DIR }}

    - name: Plan Summary
      if: always()
      run: |
        echo "### Terraform Plan Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Plan Status:** ${{ steps.plan.outcome }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ "${{ secrets.AWS_ACCESS_KEY_ID }}" = "" ]; then
          echo "AWS credentials not configured - plan skipped" >> $GITHUB_STEP_SUMMARY
        elif [ "${{ steps.plan.outcome }}" = "success" ]; then
          echo "Plan completed successfully" >> $GITHUB_STEP_SUMMARY
        else
          echo "Plan failed - check logs for details" >> $GITHUB_STEP_SUMMARY
        fi

    - name: Plan Status
      if: steps.plan.outcome == 'failure'
      run: exit 1